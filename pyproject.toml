[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nveil"
dynamic = ["version", "dependencies"]
description = "AI-powered data visualization toolkit. Describe your data, get production charts, locally."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "AGPL-3.0-or-later"}
authors = [{name = "NVEIL", email = "contact@nveil.com"}]
keywords = [
    "visualization", "data-visualization", "ai", "charts", "plotly",
    "vtk", "deckgl", "pandas", "data-science", "no-code",
    "machine-learning", "geospatial", "3d", "scientific-visualization",
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Visualization",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",
    "Typing :: Typed",
]

[project.scripts]
nveil = "nveil.cli.main:main"

[project.urls]
Homepage = "https://nveil.com"
Documentation = "https://docs.nveil.com"
Source = "https://github.com/nveil-ai/nveil-toolkit"
Issues = "https://github.com/nveil-ai/nveil-toolkit/issues"
"API Reference" = "https://docs.nveil.com/api-reference/"
Changelog = "https://docs.nveil.com/changelog/"

[project.optional-dependencies]
docs = [
    "mkdocs>=1.5",
    "mkdocs-material>=9.0",
    "mkdocs-section-index>=0.3",
    "mkdocstrings[python]>=0.24",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "e2e: end-to-end tests requiring a running server",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
nveil = [
    "skills/nveil/SKILL.md",
    ".claude-plugin/plugin.json",
    ".mcp.json",
]
